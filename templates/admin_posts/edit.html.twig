{% extends 'admin-base.html.twig' %}

{% block title %}Edit Posts{% endblock %}

{% block body %}
<div class="row justify-content-center my-4">
    <div class="col-md-6">
    <h1>Edit Posts</h1>

    {{ include('admin_posts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_posts_index') }}">back to list</a>

    {{ include('admin_posts/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
