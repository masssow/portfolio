{% extends 'admin-base.html.twig' %}

{% block title %}CategoriePosts{% endblock %}

{% block body %}
    <h1>CategoriePosts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_post.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_post.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_post.description }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ categorie_post.imageName }}</td>
            </tr>
            <tr>
                <th>ImageSize</th>
                <td>{{ categorie_post.imageSize }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ categorie_post.updatedAt ? categorie_post.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categorie_index') }}">back to list</a>

    <a href="{{ path('app_admin_categorie_edit', {'id': categorie_post.id}) }}">edit</a>

    {{ include('admin_categorie/_delete_form.html.twig') }}
{% endblock %}
