<form method="post" action="{{ path('app_admin_categorie_delete', {'id': categorie_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_post.id) }}">
    <button class="btn">Delete</button>
</form>
